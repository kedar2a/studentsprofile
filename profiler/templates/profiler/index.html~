<!-- Check wheather there is data or not -->

{% if students_data %}
	<!-- If true, data exist then process received data -->
	<h4>Here is the summary of all the students : </h4>
	
	<ul>
	{% for student in students_data %}
	
		<li><a href="{% url 'studentid' student.id %}">{{ student.first_name }} {{ student.last_name }}</a></li>
	
		<ul style="color:gray;">
	
			<li>Roll No  : {{ student.roll_no }}</li>
			<li>Email : {{ student.email }}</li>
			<li>Admission Date : {{ student.admission_date }}</li>
			<li>Fees Paid : ${{ student.fees_paid }}</li>
	
		</ul>
	
		<br/><hr/>
	
	{% endfor %}
	</ul>	

{% else %}
	<!-- If false, data dosent exist in database -->
	<h4>Sorry no entries of any student yet !</h4>
{% endif %}	


